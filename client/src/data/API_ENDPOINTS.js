const API_BASE_URL = {
  dev: "http://localhost:3001",
};

const API_ENDPOINTS = {
  GET_USERS: "/users",
  CREATE_USER: "/users",
  UPDATE_ROLE: "/users",
  SEARCH_USER: "/users",
};

export { API_BASE_URL, API_ENDPOINTS };
